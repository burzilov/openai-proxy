# Secrets (change before running)
PROXY_API_KEY=sk-proxy-local-change-me
LITELLM_MASTER_KEY=sk-litellm-master-change-me

# Everything else is hardcoded in internal/config/config.go (including auth → /data/auth.json).
